package m3.eventplanner.models;

public class Service extends Offering{
    public Service(Long id, String title, double rating, String provider, double price) {
        super(id, title, rating, provider, price);
    }
}
